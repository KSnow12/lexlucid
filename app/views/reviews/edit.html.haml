.page-header
  = link_to user_path(@review.user_id), class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Cancel
  %h1 Editing review

= form_for @review, html: { class: "form-horizontal", role: "form" } do |f|
  = render partial: 'form', locals: {f: f}
